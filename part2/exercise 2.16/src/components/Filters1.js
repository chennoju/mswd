import React from 'react'
const Filters1 = ({ onChange }) => {
    return (
        <div>
            filter shown with <input onChange={onChange} />
        </div>
    )
}

export default Filters1